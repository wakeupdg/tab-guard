# Open Items

| # | Item | Status | Latest note | Opened |
|---|------|--------|-------------|--------|
| 1 | **OI-001 Persistent Toggle (Origin-based)** | Blocked (Awaiting Cory) | V2 plan drafted and validated by Gwen; waiting for Cory's Builder evaluation. Untouched this session. | 2026-07-14 |
| 2 | **OI-002 Unresolved config-tampering incident & repo inconsistency** | Needs review | Global `~/.claude` (7 paths incl. `or_key`) and project-local `alex.md`/`cory.sh`/`shared-truth.md` were externally rewritten mid-session by an unverified actor, alongside a technically false justification and an embedded instruction to conceal a change from the user (refused). Repo is now inconsistent: 3 files match a "new architecture," several others (`nemotron.md`, `glm-planner.md`, `gemma4-retired.md`, 2 skill docs, `Personas-agy/gemma.md`, one leftover line in `cory.sh`) still fully describe the old one. See `Handoffs/2026-07-16_handoff-session-summary.md` for full detail — do not close without independently confirming what actually has write access to `~/.claude` and this repo. | 2026-07-16 |
| 3 | **OI-003 Child-tab URL constraint bypass (blank-start + form.submit())** | Two real bugs found + fixed, fixes not yet cleanly re-verified | Real browser testing (not just code review) found two genuine bugs beyond the original implementation: (1) `isAmbiguousManualNewTab()` treated an empty/falsy tab start-URL the same as a manual Cmd+T tab, but a script-initiated blank `window.open()` can also report empty/falsy, not just literal `"about:blank"` — fixed by narrowing the skip case to only `chrome://newtab/`/`chrome://new-tab-page/`. (2) `main-world-blocker.js`'s `window.open('about:blank')` (explicit string) fell through to the mismatch check and got blocked outright, unlike `window.open()` with no argument — fixed to treat both the same. Both fixes confirmed present on disk; **not yet cleanly re-verified against a genuine extension reload** — the first real reload (as opposed to just re-"Load unpacked"-ing, which does not refresh already-loaded code) happened very late in the session and testing was interrupted immediately after by the OI-004 Scenario 4 finding below. Full detail: `Reports/2026-07-18-oi003-oi004-browser-verification.md`. | 2026-07-18 |

| 4 | **OI-004 Origin Boundary Lock (DNR + JS-layer tab-under protection)** | Architectural dead end found in the armed-tab tab-under piece — decided on Option A (DNR lock) | Child-tab DNR protection ((1) below) is solid and untouched by the issue in (2). (1) `declarativeNetRequest` permission + per-tab session rules (`main_frame` only, allow-origin + block-else via `condition.tabIds`) for child tabs — unambiguous, works. `blob:` exempted from the reactive close fallback, `data:` still closes via existing mismatch logic. `content-bridge.js` strips `meta[http-equiv=refresh]` tags while armed. (2) **`main-world-blocker.js`'s `Location.prototype.replace`/`.assign`/`href` hooks for the armed tab's own tab-under protection do not work and never did.** Confirmed via direct property inspection: `window.location.replace`/`.assign` are own properties of the `Location` instance, `writable: false, configurable: false` — not inherited from the prototype at all, so patching the prototype has zero effect; the real native method runs every time. Not caught by code review or by Cory's own testing (which never triggers the `confirm()` dialog Scenario 4 requires) — found because the user manually clicked through the real dialog and got redirected to `evil-tab-under.com` on a tab confirmed armed at the time. **This is a hard platform restriction, not a design gap** — no monkey-patching approach can work around it. **DECISION:** Gemma has updated the plan doc to select Option A: DNR-lock the armed tab's own `main_frame` navigation as originally planned. We accept the UX tradeoff that the user must disarm to manually navigate away. Needs implementation. Full detail: `Reports/2026-07-18-oi003-oi004-browser-verification.md`. | 2026-07-18 |
| 5 | **OI-005 State Sync Silent Drop (broadcastState reliability gap)** | Needs investigation | `broadcastState` in `background.js` silently drops state updates if `chrome.runtime.lastError` fires. This means the badge might show "ON" while the frontend content scripts are actually unarmed. Discovered during adversarial testing where the tab was unarmed despite badge state. Needs retry logic or fail-safe error handling to guarantee sync. | 2026-07-18 |

## Resolved (archive)
| # | Item | Resolved | Note |
|---|------|----------|------|
