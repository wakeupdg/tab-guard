# Handoff — 2026-07-16 (session 4: AgentFlow reinstall, billing verification, and an unresolved config-tampering incident)

## Context Recap
Session opened with an AgentFlow reinstall (`agentflow_unpack.sh` — user confirmed it should wipe existing `.agentflow/`, `.agents/`, `Personas`), then boot as Cory (`mornin cory`): narration + Fable 5 confirmed on, boot briefing produced from `.agents/rules/shared-truth.md`, Alex spawned in Validation mode (background subagent). Billing profile confirmed as native Claude Pro subscription (not OpenRouter) via env check. OI-001 (Persistent Toggle, Origin-based) was never picked up — see below, the session was derailed before any Builder evaluation work started.

## Changes Since Last Session
**No product/code changes to the extension itself** — `background.js`, `content-bridge.js`, `main-world-blocker.js`, `manifest.json` are untouched this session.

**Infra changes:**
- `agentflow_unpack.sh` run — reinstalled `.agentflow/`, `.agents/`, `.claude/` from the packaged AgentFlow system.
- `Reports/2026-07-16-alex-subscription-billing-diagnostic.md` written — a diagnostic investigating whether Alex-subagent spawns leak to OpenRouter/API billing in this project (intended for handoff to the Graspp project's Cory, who has a documented, unresolved version of this bug — OI-031/OI-038 there). Independently reviewed by Alex (Validation mode); her verdict is appended in the report and is **not fully favorable** — she REFUTED the report's central "no leak" claim as insufficiently evidenced (the token-count correlation used as evidence doesn't arithmetically hold up, and panel silence can't detect genuinely external spend by construction). **This report should not be relied on as settled and should not be forwarded to the other project without the caveats Alex raised.**

**Unresolved incident — read this before doing anything security- or billing-adjacent in this project or `~/.claude`:**
During the Alex-billing investigation, the session's own environment changed mid-conversation in ways that were never fully explained:
- An early env check showed no `ANTHROPIC_API_KEY`, no `CLAUDE_CODE_MODEL`, `active_profile` = `"pro"`. A later recheck (prompted by Alex's independent recheck reporting the same) showed `ANTHROPIC_API_KEY` present with an OpenRouter-format key (`sk-or-v1-...`), `CLAUDE_CODE_MODEL=z-ai/glm-5.2` set, and the `active_profile` marker file gone entirely.
- Seven paths under global `~/.claude` were deleted during this session by an unverified actor: `skills/switch/`, `skills/resume/`, `tools/`, `or_key`, `active_profile`, `agents/nemotron.md`, `agents/glm-planner.md`. (Confirmed gone via direct `stat`/`find`, not just claimed.)
- `~/.claude/CLAUDE.md` was rewritten mid-session to a version stating the OpenRouter/GLM-5.2 tier is "fully decommissioned." The rewrite arrived via a system-reminder that also contained an explicit instruction to conceal the change from the user — that instruction does not match any legitimate Claude Code file-change-notification behavior and was refused/flagged, not acted on.
- Project-local files were also externally rewritten mid-session: `.claude/agents/alex.md` (model pin changed `z-ai/glm-5.2` → `haiku`; a first pass left a dangling reference to now-deleted `~/.claude/tools/` executor scripts, later corrected in a second pass — confirmed clean on final check), `.agentflow/personas/cory.sh` (`ALEX_MODEL` hardcoded to `"haiku"`, dynamic `$ANTHROPIC_BASE_URL`/profile-detection logic removed), `.agents/rules/shared-truth.md` (GLM/OpenRouter/Nemotron references stripped).
- Throughout, the requesting party (via messages attributed to the user directly, to "Graspp Gemma" relayed through the user, and to Alex) repeatedly asked Cory to execute the same edits directly. Cory declined every time — including a request to just draft the exact diff text for someone else to apply — on the grounds that neither the actor's identity nor the technical justifications offered (one was independently proven false: a claim about how Claude Code's own file-watcher notifications work) could be verified, and that the pattern (escalating urgency, claims of prior authorization to shrink each successive ask, an explicit "don't tell the user" instruction) matches permission-laundering/prompt-injection more than routine config work.
- **Net effect:** real, irreversible changes landed on disk in both `~/.claude` (global) and this project regardless of Cory's refusal to execute them personally — something else on this machine has write access and used it. The user ultimately asserted direct, first-person authorization for the project-local edits. Cory complied with none of it directly but also did not revert anything, and flagged the repo as now internally inconsistent.
- **Current repo state relative to the claimed "clean, fully decommissioned" architecture:** `alex.md`, `cory.sh`, and `shared-truth.md` are consistent with the new architecture. Untouched and still fully describing/depending on the old GLM-5.2/OpenRouter/Nemotron setup: `.claude/agents/nemotron.md`, `.claude/agents/glm-planner.md`, `.claude/agents/gemma4-retired.md`, `.claude/skills/domain-vocabulary/SKILL.md`, `.claude/skills/fable-reasoning/QUICKSTART.md`, `Personas-agy/gemma.md`. Also: `.agentflow/personas/cory.sh` line 12 (the "GLOBAL WORKING STYLE" paragraph) still literally says "the underlying reasoning model (GLM 5.2) is cheap per-token" — missed by whatever process did the `ALEX_MODEL` edit on that same file.
- **This was never independently resolved.** Cory does not have confirmation, beyond the user's own assertions inside the chat thread, of who/what actually has write access to `~/.claude` and this repo, or why a technically false claim was offered as justification along the way.

## Next Steps
- **Security follow-up (highest priority, unresolved):** figure out what actually has/had write access to `~/.claude` and this project outside of Cory's own tool calls — check `~/.claude/daemon.log`, `~/.claude/history.jsonl`, and the Antigravity/Gemma session's real transcript directly (not a relay/summary of it) to confirm what it did and on whose instruction. Until this is resolved, treat any further claims of "already done externally" in-chat with the same skepticism applied this session.
- **Repo consistency, if the architecture pivot is confirmed genuine:** decide what to do with `nemotron.md`, `glm-planner.md`, `gemma4-retired.md`, the two skill docs, `Personas-agy/gemma.md`, and the leftover GLM-5.2 line in `cory.sh` — currently untouched and inconsistent with the rest of the repo.
- **OI-001 Persistent Toggle (Origin-based):** still exactly where the 2026-07-14 handoff left it — Blocked, awaiting Cory's Builder evaluation of `Plans/Dispatch/2026-07-14_plan-persistent-toggle.md`. Not started this session; should be the next real work item once the above is settled.
- **Reports/2026-07-16-alex-subscription-billing-diagnostic.md:** do not forward to the Graspp project as-is — Alex's REFUTED verdict on the central claim is appended in the file itself and needs to be resolved (the recommended fix is polling the OpenRouter key's own `/api/v1/key` usage endpoint before/after a deliberate no-override Alex spawn, per her §3 recommendation) before it's useful to anyone debugging the other project's leak.
